{
  "name": "juce-server-autostart",
  "displayName": "JUCE Server Autostart Extension",
  "description": "Automatically starts JUCE AI servers (Llama, Embedding, MCP) when VS Code loads.",
  "version": "0.0.5",
  "publisher": "artqcid",
  "repository": {
    "type": "git",
    "url": "https://github.com/artqcid/vscode-autostart-extension"
  },
  "engines": {
    "vscode": "^1.108.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "serverAutostart.startServers",
        "title": "Server Autostart: Start All Servers"
      },
      {
        "command": "serverAutostart.stopServers",
        "title": "Server Autostart: Stop All Servers"
      },
      {
        "command": "serverAutostart.showStatus",
        "title": "Server Autostart: Show Server Status"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "JUCE Server Autostart Configuration",
      "properties": {
        "juce-server-autostart.mcpServerPath": {
          "type": "string",
          "default": "C:\\Users\\marku\\Documents\\GitHub\\artqcid\\ai-projects\\mcp-server-misc",
          "description": "Path to JUCE MCP Server directory"
        },
        "juce-server-autostart.embeddingServerPath": {
          "type": "string",
          "default": "C:\\Users\\marku\\Documents\\GitHub\\artqcid\\ai-projects\\embedding-server-misc",
          "description": "Path to JUCE Embedding Server directory"
        },
        "juce-server-autostart.llamaScriptPath": {
          "type": "string",
          "default": "C:\\Users\\marku\\.continue\\llama-vscode-autostart.ps1",
          "description": "Path to Llama autostart PowerShell script"
        },
        "juce-server-autostart.autoStartDelay": {
          "type": "number",
          "default": 2000,
          "description": "Delay in milliseconds before starting servers on VS Code activation"
        },
        "juce-server-autostart.mcpStartDelay": {
          "type": "number",
          "default": 3000,
          "description": "Additional delay in milliseconds before starting MCP server"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.108.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.52.0",
    "eslint": "^9.39.2",
    "esbuild": "^0.27.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
